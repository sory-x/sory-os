# Build & Publish SoryOS APT - GitHub CI Auto-Build & Push to APT Repo GitHub Pages  ## Commande unique pour déclencher le build CI + publication  ```bash  # Commit + push déclenche automatiquement le CI GitHub Actions  # Le CI build tous les .deb (23 integration + 28 COSMIC + cosmic-sound-theme)  # et les publie sur le dépôt APT GitHub Pages (sory-x/soryos-apt)  git add -A && git commit -m 