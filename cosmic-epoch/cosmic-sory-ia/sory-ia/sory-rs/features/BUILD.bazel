load("//:defs.bzl", "sory_rust_crate")

sory_rust_crate(
    name = "features",
    crate_name = "sory_features",
    compile_data = glob(
        include = ["**"],
        exclude = [
            "BUILD.bazel",
            "Cargo.toml",
        ],
        allow_empty = True,
    ),
)
