load("//:defs.bzl", "sory_rust_crate")

sory_rust_crate(
    name = "plugin",
    crate_name = "sory_plugin",
    compile_data = glob(
        include = ["**"],
        exclude = [
            "**/* *",
            "BUILD.bazel",
            "Cargo.toml",
        ],
        allow_empty = True,
    ),
)
