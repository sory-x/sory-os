load("//:defs.bzl", "sory_rust_crate")

sory_rust_crate(
    name = "exec",
    crate_name = "sory_exec",
    test_shard_counts = {
        "exec-all-test": 8,
    },
    test_tags = ["no-sandbox"],
)
