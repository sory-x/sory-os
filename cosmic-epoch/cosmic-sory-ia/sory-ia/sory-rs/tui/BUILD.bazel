load("//:defs.bzl", "MACOS_WEBRTC_RUSTC_LINK_FLAGS", "sory_rust_crate")

sory_rust_crate(
    name = "tui",
    crate_name = "sory_tui",
    compile_data = glob(
        include = ["**"],
        exclude = [
            "**/* *",
            "BUILD.bazel",
            "Cargo.toml",
        ],
        allow_empty = True,
    ) + [
        "//sory-rs/collaboration-mode-templates:templates/default.md",
        "//sory-rs/collaboration-mode-templates:templates/plan.md",
    ],
    test_data_extra = glob([
        "src/**/*.rs",
        "src/**/snapshots/**",
    ]),
    integration_compile_data_extra = ["src/test_backend.rs"],
    extra_binaries = [
        "//sory-rs/cli:sory",
    ],
    rustc_flags_extra = MACOS_WEBRTC_RUSTC_LINK_FLAGS,
    test_shard_counts = {
        "tui-unit-tests": 8,
    },
)
